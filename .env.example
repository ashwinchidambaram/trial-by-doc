# API keys — presence checked by core/secrets.py; values NEVER logged.

# The two keys the current workflows actually use:
HF_TOKEN=                       # Hugging Face — model/dataset downloads (verify-env checks it)
OPEN_ROUTER_API_KEY=            # OpenRouter — the B.2 API reader rungs (--reader gpt5mini / haiku45)

# API OCR models (adapters wired, runs deferred — see README Gaps):
MISTRAL_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AZURE_DOCINTEL_ENDPOINT=
AZURE_DOCINTEL_KEY=
GOOGLE_APPLICATION_CREDENTIALS=
